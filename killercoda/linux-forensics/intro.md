## NFCSOC Labs — Linux Server Forensics: SSH Compromise

**Incident Alert — CRITICAL**

Production web server NFC-SRV-WEB1 is showing unusual outbound connections and suspicious cron activity.

**Your mission:**
1. Identify how the attacker got in (attack vector)
2. Find all malware and persistence mechanisms
3. Determine what data was exfiltrated
4. Decode the attacker payload to capture the flag

Use `show_hints 1` through `show_hints 5` if you get stuck.
